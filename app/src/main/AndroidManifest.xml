<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cst2335.covid19casedata_teamproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".utils.CustomApplication"
        android:theme="@style/Theme.Covid19CaseData_teamProject">
        <activity
            android:name=".ui.activity.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.CaseDataListActivity"
            android:exported="false"
            android:label="@string/title_activity_case_data_list"
            android:theme="@style/Theme.Covid19CaseData_teamProject.NoActionBar"/>

    </application>

</manifest>